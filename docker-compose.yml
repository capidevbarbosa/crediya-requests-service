services:
  postgres:
    image: postgres:latest
    container_name: crediya_postgres_resq
    environment:
      POSTGRES_DB: request_db
      POSTGRES_USER: crediya_user
      POSTGRES_PASSWORD: crediya_password
    ports:
      - "5434:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: